/* DeviceMemoryPool.cu  –  Compiled translation unit.
 * The implementation is header-only (DeviceMemoryPool.cuh) but
 * this TU is needed so the static library has an object file to link. */
#include "DeviceMemoryPool.cuh"
